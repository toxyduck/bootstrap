# toxy bootstrap

```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/toxyduck/bootstrap/main/install.sh)"
```

The installer supports Ubuntu/Debian and runs inside tmux. Machine inventory and
secrets stay in the private `toxyduck/.dotenv` repository.
